// Generated by CoffeeScript 2.2.2
(function() {
  $(document).ready(function() {
    return $('#choicefiles').on('change', function(e) {
      var f, files, html, i, len, parent, results;
      files = $('#choicefiles')[0].files; // it is a list
      parent = $('#choicefiles').parent();
      results = [];
      for (i = 0, len = files.length; i < len; i++) {
        f = files[i];
        //alert 'you choice file is:' + f.name + ' size:' + f.size 
        html = '<p class="help-block">' + f.name + '/size ' + f.size + '</p>';
        results.push(parent.append($(html)));
      }
      return results;
    });
  });

}).call(this);
