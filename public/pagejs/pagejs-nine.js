// Generated by CoffeeScript 2.3.2
(function() {
  // Notice that,"$(document).ready" or "$ ->" is not fit svg document load state.should begin with below line.
  window.onload = function() {
    var $ul, circle, draw, i, j, len, ref;
    draw = $('#you-guess')[0].getSVGDocument();
    circle = $(draw).find('#thecircle');
    $ul = $('<ul/>', {
      'class': 'list-group'
    });
    ref = ['cx=' + circle.attr('cx'), 'cy=' + circle.attr('cy'), 'r=' + circle.attr('r')];
    for (j = 0, len = ref.length; j < len; j++) {
      i = ref[j];
      $ul.append($('<li/>', {
        'class': 'list-group-item',
        text: i
      }));
    }
    return $('body').append($ul);
  };

}).call(this);
