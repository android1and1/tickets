// Generated by CoffeeScript 2.3.1
(function() {
  $(function() {
    var current;
    current = 1;
    $('.previous').on('click', function(evt) {
      // may change current's value.
      if (current > 1) {
        current--;
        $(this).attr('href', '#p' + current);
        return $(this).tab('show');
      } else {

      }
    });
    //alert 'it is already the 1st page.'
    return $('.next').on('click', function(evt) {
      // total 4 pages
      if (current < 4) {
        current++;
        $(this).attr('href', '#p' + current);
        return $(this).tab('show');
      } else {
        //alert 'it is already the last page.'
        return $('body').append(alertbox);
      }
    });
  });

}).call(this);
