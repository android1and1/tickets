// Generated by CoffeeScript 2.3.2
(function() {
  $(function() {
    return $('.remove').on('click', function(evt) {
      var funcButton, id;
      id = $(this).data('itemid');
      funcButton = $('<button/>', {
        'class': "btn btn-warning btn-md",
        type: "button",
        'data-dismiss': "modal",
        text: 'Ensure For Deleting This Item',
        id: 'iensure'
      });
      window.createModal($('body'), {
        titleText: 'Do You Want To Delete One Item?',
        bodyText: 'Click Ensure Will Execute Deleting.',
        boxid: 'said',
        'funcButton': funcButton
      });
      $('#said').modal();
      return $('#iensure').on('click', function(evt) {
        return $.ajax({
          dataType: 'json',
          url: '/superuser/del-admin?id=' + id,
          type: 'PUT'
        }).done(function(json) {
          // use 'flash' skill,global func variable - createAlertBox()
          // is already included.
          return window.createAlertBox($('body'), json.gala);
        }).fail(function(one, two, three) {
          return window.createAlertBox($('body'), json.reason);
        });
      });
    });
  });

}).call(this);
