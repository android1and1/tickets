// Generated by CoffeeScript 2.3.1
(function() {
  $(function() {
    var formData;
    formData = new FormData;
    // init form data
    $('input.car_field').each(function(ind, ele) {
      // ele = this = @
      return formData.set($(this).attr('name'), $(this).val());
    }).on('change', function(evt) {
      return formData.set($(this).attr('name'), $(this).val());
    });
    $('.nav.nav-tabs a').on('click', function(e) {
      e.preventDefault();
      return $(this).tab('show');
    });
    $('.nav.nav-tabs a').eq(0).tab('show');
    return $('#commit').on('click', function(e) {
      var entry, iterator, results;
      iterator = formData.entries();
      results = [];
      while ((entry = iterator.next()) && !entry.done) {
        results.push(alert(entry.value));
      }
      return results;
    });
  });

}).call(this);
