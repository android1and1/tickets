// Generated by CoffeeScript 2.4.1
(function() {
  var Browser, app, browser, http, server;

  Browser = require('zombie');

  Browser.localhost('example.com', 4140);

  browser = new Browser;

  app = require('../app');

  http = require('http');

  server = http.Server(app);

  server.listen(4140);

  server.on('error', function(err) {
    return console.error(err);
  });

  // sometimes it will help(below line)
  //browser.waitDuration = '30s'
  describe('zombie ability display::', function() {
    after(function() {
      return server.close();
    });
    return describe('access index page should success::', function() {
      before(function() {
        return browser.visit('http://example.com');
      });
      it('should access page successfully::', function() {
        return browser.assert.success();
      });
      //No Need This Time
      //console.log browser.html()
      it('this page has more than 10 link tags::', function() {
        return browser.assert.elements('a', {
          atleast: 10
        });
      });
      return it('page title is "welcome-daka"::', function() {
        return browser.assert.text('title', 'welcome-daka');
      });
    });
  });

}).call(this);
