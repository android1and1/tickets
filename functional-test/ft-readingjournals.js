// Generated by CoffeeScript 2.1.1
(function() {
  var Browser, app, assert, browser, http, server;

  Browser = require('zombie');

  http = require('http');

  assert = require('assert');

  Browser.localhost('www.cinema.com', 4140);

  browser = new Browser;

  browser.waitDuration = '30s';

  app = require('../app.js');

  server = http.Server(app);

  server.on('error', function(error) {
    console.log('during test occurs *error*::');
    return console.error(error.message);
  });

  server.listen(4140);

  // start assertions.
  describe('Router Functional Test -  /reading-journals::', function() {
    before(function(done) {
      return browser.visit('http://www.cinema.com/reading-journals', done);
    });
    return it('index page should be accessible::', function() {
      return browser.assert.success();
    });
  });

}).call(this);
