extend layouts/main
block content
  h2.code.user(data-user=user,data-code=code) [Response] #{user} #{status}
  h2 错误原因 #{JSON.stringify(reason)}
  nav
    ul.pager
     li <a href="/"> 回到首页</a>
block pagejs
  script(src="/socket.io/socket.io.js")
  script(src="/mine/mine-user-daka-response.js")
