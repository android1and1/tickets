//-
  估没到喱个组件呢其可以从V3,无错地运行至V4.
  variables:
  1, 'alertStyle'
  2, 'alertContent
  retrieved from server's route codes. 
  
  ****
  skills-1
  if var1 or var2 is not given,use default value in this template.
  via "alertStyle 
- var as_alertStyle = alertStyle || 'alert-info'
div(class="alert alert-dismissible fade show " + as_alertStyle,role="alert")
  button(type="button" class="close" data-dismiss="alert" aria-label="Close")
    span(aria-hidden="true") &times;
  h3 #{alertContent||'All Is Default,Includes Alert Type(Colour).'}

