extend layouts/main
block pagejs
  script(src="/mine/mine-createModal.js")
block footer
  h2 show a dialog -- bt js-suit : modal.js
  //button(data-toggle="modal" data-target="#myModal" class="btn btn-default btn-lg") show
  button(data-target="#myModal" data-toggle="modal" class="btn btn-default btn-lg") show
  script.
    $(function(){
      //- need create it first.     
      //createModal($('body'),{titleText:'i love xiuxiu',bodyText:'long long ago..'});
      createModal(
        $('body'),{
            titleText:'i love xiuxiu',
            bodyText:'long long ago..',
            modalID: 'modal47',
            funcButton: $(
              '<button/>',
              {
                'id':"yijia",
                'class':"btn btn-md btn-success",
                //'role':'button',
                'text':"Save",
                'data-dismiss':'modal',
              }
            )//funcButton:$(..)
          }
      );
      //  then javascript code trigger this component.
      $('#yijia').on('click',function(evt){
        $.ajax({url:'/tricks/gethappy',data:{id:123},type:'GET',dataType:'json'})
        .done(function(json){
            $('<h1>').text(json.received).appendTo('body');
          }) 
      });
 
      $('#myModal').modal();
    });
